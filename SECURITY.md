# 安全说明 / Security policy

WebCodex 面向一位本机所有者，通过 MCP 向其 ChatGPT 连接提供授权项目工具。它不是多租户隔离服务，也不是操作系统沙箱。

## 信任与访问边界

- 文件工具按已登记工作区、设备身份和路径策略授权；配置、凭据目录、状态数据及 Git 管理目录受到保护。只读限制覆盖服务提供的写入工具。
- `fs_copy` 在同一设备的授权工作区间直接复制原字节，分别核对源和目标身份、源哈希及目标覆盖条件。源可以只读，目标必须可写；它不经过远端下载、不要求开启命令执行，也不授予其他目录权限。
- 不要将较宽的可写父工作区与较窄的只读目录混用来表达全局拒绝：其他有效工作区可能仍授予访问。规则和重叠校验见[配置指南](docs/local-configuration.md)。
- 新安装按项目目标默认使用 `trusted-host` 和 `commandPolicy=all`，初始工作区可读写；本机原生程序名或绝对路径无需逐项放行，并以启动 WebCodex 的账户权限运行。程序可能产生文件或网络副作用，且工作区路径检查不能约束子进程内部行为。它不会自动获得管理员权限或绕过 NTFS/UAC。升级已有配置不会静默扩权；未声明策略的旧配置保留 `allowlist`，只有本机用户明确使用“一键完全开放”或 `access full` 才会改变。
- Codex 历史读取需在本机开启，只读取支持的可见记录并做凭据脱敏。历史及项目文件均是不可信输入，不能据此扩大权限或自动执行旧命令。
- HTTP MCP 和本地控制中心只绑定 loopback，使用独立端口和凭据。HTTP bearer token 不是公网 OAuth；不要未经设计将端口暴露到公网。
- 控制中心可修改权限和配置，是本机管理入口；使用独立临时面板令牌、严格同源 JSON 写请求和配置版本检查。密钥不回显、不存入浏览器长期存储，管理 API 不暴露为 MCP 工具。页面只控制自行启动的服务，不按外部记录的 PID 终止进程。
- `connect` 默认输出带临时面板令牌的完整本机链接。令牌与面板进程同寿命，应避免进入公开日志、issue 或截图。页面读取 URL fragment 后清除地址栏片段，凭据仅保留于该页面的 `sessionStorage` 或内存；不使用长期 `localStorage`。重启 MCP 服务不等于重启面板进程。
- 官方隧道需要 OpenAI 的账户和组织授权。上传或返回的文件内容会离开本机并进入所选宿主，使用前应了解该服务的数据处理条款。
- PDF 正文原型把授权 PDF 的完整原字节传入宿主组件，在浏览器解析并经普通工具返回分页正文。私有能力票据不进入模型正文；字节哈希不等于对组件提交文本的独立语义验证。文档和缓存有期限，每次访问重新检查工作区策略。提取内容仍是不可信输入。
- 配置、备份、任务记录和程序输出可能包含敏感内容。日志尚无统一自动保留清理策略，应由本机所有者管理。

## 凭据与仓库

默认生成文件保存入口 `fs_save_file` 只接收实际宿主文件 ID 或宿主提供的官方文件对象。只有 ID 时，组件通过 `getFileDownloadUrl` 私下取得临时地址，再交给本机服务；这不授予服务访问 ChatGPT 沙箱的权限。组件没有文件选择、上传或直接下载步骤，不把票据、文件 ID、签名 URL 或原字节加入模型上下文。工具定义或 helper 存在不能证明当前宿主已授权某个生成文件。

自动保存和旧 `fs_import_file` 共用下载器，当前仅允许 `https://files.oaiusercontent.com`；不接受 `sandbox:/...`、本机路径或任意第三方 URL。每次连接/重定向检查公开网络目标、TLS、大小与完整性。统一配置的 `tunnel.proxyUrl` 可用于 CONNECT，签名 URL 仅在官方主机 TLS 校验后发送；不从工具参数接受代理，也不使用 OpenAI API key 下载文件。该出站下载不增加公网监听、证书或第三方隧道。

自动保存固定原件大小和 SHA-256，下载后写入前不符即拒绝，不用下载所得其他字节反向替换原件摘要。票据绑定设备、工作区身份、目标和覆盖条件，只保留在有界内存；重启后未完成票据失效，持久结果仍可查询。过期或矛盾的组件元数据不会改为自动提交另一个文件。

文件参数、下载 URL 和文件标识不写入普通结果或诊断；保存文件不会自动执行它。只创建、旧目标哈希冲突、备份、设备和工作区权限仍生效。`saved/verified=true` 表示记录的提交结果，当前文件状态须另行检查；提交后校验失败属于不确定状态，不能因返回错误就断言没有写入。先查询原操作，不能换操作键或换工具自动重放。详细状态与重试规则见[回存说明](docs/file-writeback.md)。

真实配置位于被忽略的本机目录，不应提交。CLI 创建/编辑配置时限制文件权限；手工编辑应保留这些权限。不要将 API key 放入命令行参数、对话、issue 或截图。

公开模板只放空凭据。`config show` 返回有限的脱敏视图，仍可能含设备名和路径；对外发送前继续检查。`npm run audit:repo` 仅检查 Git 候选文件，不扫描你的被忽略配置，也不能保证发现所有秘密。

泄露凭据后应先撤销或轮换，再处理仓库历史；仅删除当前文件不能使已发布凭据失效。

## 报告漏洞

发布仓库并开启 GitHub Private Vulnerability Reporting 后，请通过仓库 **Security → Report a vulnerability** 私下报告。若此入口尚不可用，请先通过普通 issue 请求私密联系渠道，不公开利用步骤、密钥或真实个人文件。当前尚未设置专用安全邮箱。

请提供受影响版本、平台、最小合成复现和影响说明。当前维护范围为最新预览版，不承诺旧版本安全回补或固定响应时限。

For sensitive reports, use GitHub private vulnerability reporting when enabled. If unavailable, request a private contact channel without disclosing exploit details or credentials publicly.
