# 测试与验证边界

## 0.16.0-preview.7 跨平台 CI 修正

2026-09-12，最终源码提交 `00772c7` 的[三平台完整 CI](https://github.com/xq3427/WebCodex/actions/runs/34621929215)全部通过：

| 平台 | 测试总数 | 通过 | 失败 | 跳过 |
| --- | ---: | ---: | ---: | ---: |
| Windows | 1,024 | 1,024 | 0 | 0 |
| Linux | 1,024 | 1,019 | 0 | 5 |
| macOS | 1,024 | 1,019 | 0 | 5 |

Linux/macOS 的 5 项跳过均为 Windows 专属测试；三个平台的符号链接测试均已实际执行通过。TypeScript、构建、65 项工具定义、6 项仓库检查专项及 232 个公开候选文件审计也全部通过。后续仅同步这份测试记录，不修改被验证的源码或工作流。此结果覆盖代码及合成测试；真实 ChatGPT PDF 正文和自动原件回存仍待宿主验收，本轮没有重启真实服务。

2026-09-12，核对公开提交 `6f1cd72` 的[三平台运行日志](https://github.com/xq3427/WebCodex/actions/runs/34618122225)：Windows 为 1,004 项、995 通过、9 失败；Ubuntu 为 1,004 项、998 通过、3 失败、3 平台跳过；macOS 为 1,004 项、997 通过、4 失败、3 平台跳过。失败集中在下面五类，不能把此前本机补测当作远端通过。

- **npm 10 / Windows**：使用与 CI 相同的 npm 10.9.2 在隔离合成项目中复现，仅输出脚本标题便退出 1。补入由 `SystemRoot` 派生的 `ComSpec` 后，原复现成功执行构建；不接受父进程或配置中的任意解释器覆盖。
- **Windows 配置权限**：发现旧脚本的 `New-Object` 仍触发模块自动加载。使用不可加载的合成模块目录可稳定复现失败；改用纯 .NET 构造并禁用自动加载后，真实 CLI 回归成功保存配置，核对仅预期主体具有 FullControl，且移除了 Everyone 和继承权限。此复现证明并移除了模块依赖；后续 Windows CI 中原有失败均已通过。
- **POSIX 组件发布**：原子替换会使已打开旧文件的链接数降至 0 并更新 ctime，旧读取器误报损坏。现在允许符合条件的完整旧快照，继续校验大小、mtime、链接状态及提交的 SHA-256；增加确定的元数据回归和并发完整字节断言。
- **POSIX 目录识别**：目录的正常多链接计数不再被当作普通文件硬链接；两项回归核对 `NOT_A_FILE` 和无写入，原符号链接及硬链接限制保留。
- **macOS 迁移测试**：移除不保证执行先后的 `fs.watch` 触发，明确在备份后、最终校验前修改来源。四类配置来源分别覆盖内容变化与同字节替换，共 8 个场景，校验冲突、备份和临时文件清理。

本轮统一构建通过；覆盖全部首次失败文件及相关文件操作、组件资源、作业执行的 17 个测试文件，共 **183 项：180 通过、0 失败、3 跳过**，退出码 0。3 项跳过均为本机 Windows 文件符号链接权限不足，未请求 UAC，不计为通过。npm 10.9.2 的原合成复现通过；65 项工具定义核对、仓库检查专项 6/6、232 个公开候选文件审计均通过。此为有针对性的修正回归，三平台完整结果以修正提交对应的 CI 为准。真实 ChatGPT 文件读写验收范围不变，不重启现有服务。

提交 `954be72` 的[后续 CI](https://github.com/xq3427/WebCodex/actions/runs/34620622988)中，Windows 为 **1,021 项全部通过、零跳过**；Linux 为 1,016 通过、5 平台跳过，全部检查通过；macOS 为 1,021 项、1,015 通过、1 失败、5 平台跳过，仅组件并发发布仍失败。进一步用真实原子替换、固定调用时序复现 `lstat → open` 之间合法清单换代被拒的问题。POSIX 改为使用 `O_NOFOLLOW | O_NONBLOCK` 打开后验证同一文件句柄；Windows 保留原路径身份检查，不增加重试次数，大小仍在分配缓冲区前核对。新增安全的固定错误分类及原子替换、句柄大小上限、真实硬链接拒绝测试；统一构建与组件相关 **61/61** 回归通过。

## 0.16.0-preview.7 源码发布复核

2026-09-11，将通过公开候选审查的 232 个文件复制到独立目录，不携带旧 `node_modules`、`dist`、真实配置、state 或私人文件。从现有 npm 缓存按锁文件重新安装 104 个包，TypeScript 检查和组件构建通过；在包含中文、空格的临时工作区完成初始化、配置校验与 doctor。随后在普通目录中的源码副本按 README 再次完成 `init --workspace .`、配置校验及 doctor，不启动真实隧道。离线安装验证不代表首次联网下载或其他操作系统已通过。

干净副本完整回归 **1,004 项：999 通过、1 失败、4 跳过**。唯一失败来自验证副本被放在服务保留的 `.webcodex` 目录内：跨卷工作区测试将该副本作为工作区，按策略返回 `PATH_DENIED`。将该隔离副本移至普通验收目录后，未修改产品代码或路径保护策略，整个工作区路由测试文件复测通过；直接在源码目录初始化也通过。这是测试环境修正后的补测，不描述为第二次全量零失败运行。4 项跳过仍为 Windows 文件符号链接权限不足，未请求 UAC。

工具定义核对 **65 项通过**；仓库检查专项 **6/6 通过**；公开候选审计 **232 个文件、0 问题**。审计新增真实隧道标识检查、测试目录中的个人路径检查，并为合成 PDF 样本绑定完整 SHA-256。两份本机私人验收文件保留原位，通过本地 Git 排除列表移出发布候选；不复制或上传配置、密钥、运行日志及私人文件。

PowerShell 下旧 `npm run export:tools -- --check` 可能将检查参数传给 npm 自身而执行生成。现提供独立命令 `npm run check:tools`，已实际验证只检查工具文档；README、贡献指南和 CI 已统一使用该命令。

GitHub 上首次发布提交的三平台 CI 均在 `npm test` 失败；后续取得日志并开展跨平台修正，见本页上方记录。本节仅为本机源码发布复核。真实 ChatGPT PDF 正文及自动原件回存仍未完成验收，本次不重启真实服务。

## 0.16.0-preview.7 自动原文件保存

2026-09-11，Windows / Node.js 22.16.0。新增 `fs_save_file` / `fs_save_file_status`：模型提供当前产物的真实宿主文件 ID 或官方对象；组件通过官方 `getFileDownloadUrl` 私下交接临时地址，服务下载完整原件，写入前核对原件大小和 SHA-256，覆盖时核对已有目标哈希。地址和票据不进入普通工具结果，不经模型转抄 Base64，不增加文件选择、浏览器扩展或公网服务。

新增保存服务、组件脚本和 MCP 集成专项共 **51 项通过**。使用完整 2,341,397 字节的合法合成 PNG，核对原字节、SHA-256、PNG CRC 和解压像素；覆盖源文件不符、覆盖冲突、错误设备、只读权限、票据与会话绑定、并发重复完成、重启恢复、未知结果不重放、官方对象直传、状态查询参数可直接调用，以及过期组件元数据不触发错误文件保存。宿主 helper 和下载源由测试模拟，这些结果不能证明真实 ChatGPT 已授权当前生成文件。

最终 TypeScript 与组件构建通过；65 项工具定义（55 普通、10 组件私有）重新导出并校验通过，服务指引 5,470 字符，未超出 5,500 字符预算。

首次完整回归 **1,004 项：988 通过、12 失败、4 跳过**。12 项失败涉及复制模块的测试夹具缺少依赖、有限旧组件 URI 和新增资源清单、以及首段工具指引的旧期望。补齐夹具依赖和资源期望、恢复既有说明句并更新默认保存路线断言后，重新构建，复跑全部出现失败的 5 个测试文件及新保存、路由和导入恢复相关文件，最终 **90 项：90 通过、0 失败、0 跳过**，退出码 0。所有初次失败项均已覆盖复测；这不是第二次全量零失败运行。首次全量的 4 项跳过均因当前 Windows 进程缺少文件符号链接权限，本轮没有请求 UAC 或修改系统设置。

真实宿主验收尚未完成。用户已确认原图有实际文件 ID，但尚无官方 helper 成功或本机保存回执；本机只读检查确认原目标仍为旧文件。浏览器控制工具仍无法读取验收页面，真实服务尚未加载 preview.7。不能把 ID 存在、组件渲染或合成测试通过记为原图已写回。后续应按[回存验收](file-writeback.md#真实-chatgpt-验收)取得实际 `saved/verified` 结果并再次核对目标大小和完整 SHA-256。

## 0.16.0-preview.6 导入拒绝后的原图回写指引

2026-09-11，只读诊断确认实际服务已经运行 preview.5，最新两次调用均为 `fs_import_file`，没有调用 `fs_write_binary_chunk`。持久记录均为 `failed`、`mutation_started=0`、`content_bytes=null`，固定拒绝分类为 `source_url / host_not_allowed`。因此这是 WebCodex 下载来源规则拒绝，并非宿主阻止分块请求或本机分块限额不足。诊断未输出下载 URL、文件 ID 或图片内容。

preview.6 对齐结构化能力与错误恢复：生成文件和图片明确默认使用 `fs_write_binary_chunk`；兼容导入标记为非默认入口。下载来源拒绝说明工具名、规则归属和写入前失败阶段；先查原导入操作状态，确认失败后，只有 Code Interpreter 仍持有完整原文件时才分块传送。覆盖必须使用目标当前哈希；来源规则与权限校验未放宽，未增加自动下载或跨工具重试。

新增真实 MCP Client／Server 协议测试，分别验证非允许 HTTPS 主机及 sandbox 引用被拒。使用隔离合成 PNG：目标 2,667 字节，原文件 1,565,188 字节。确认来源拒绝时无 DNS/HTTP 请求、无目标改动，再用 24 个 64 KiB 分块传输，最后一块为 57,860 字节；提交前旧图不变，完成后逐字节、SHA-256、PNG CRC、尺寸及解压像素一致，并实际通过变更预览／恢复还原旧图。此前导入失败记录保持失败，不冒充后续分块写入回执。

构建通过；下载策略／代理、文件导入、持久操作、单消息与分块写入、新恢复流程、stdio／HTTP 大图传输、状态路由、诊断及组件 URI 兼容共 **137 项通过，0 失败，0 跳过**。61 项工具定义重新导出并校验通过。本轮未重跑全量回归，未修改真实配置、重启真实服务或覆盖真实图片。该结果验证本机协议链路，不能代替 ChatGPT 真实原图字节回传验收。

## 0.16.0-preview.5 大块原字节回写

2026-09-11，Windows / Node.js 22.16.0。默认回写块从 12 KiB 提高至 64 KiB，配置最大 256 KiB；分块整文件改用 `min(binaryWriteMaxBytes, maxCacheBytes)`，默认 32 MiB，暂存总预算默认 64 MiB。单消息 `fs_write_binary` 仍默认 256 KiB。分块收齐后直接交付校验后的 Buffer，不再重编码整文件并经由单消息入口提交。

最终构建和 61 项工具定义导出／一致性检查通过。实际协议测试使用超过 3 MiB 的有效 1024×1024 合成 PNG，覆盖 stdio／HTTP 各 64 KiB、256 KiB 分块，共 4 项均通过；最大分块用例将文本及单消息二进制限额降至 1 KiB，确认 schema、HTTP 请求预算与分块整文件限额独立。核对了全部原字节、SHA-256、PNG CRC、解压像素与尺寸，以及最后一块前不落地、状态查询、重复末块、只读和设备身份拒绝。未使用或修改用户的原图。

后端测试另覆盖超过 2 MiB 文件跨重启续传、原 12 KiB 显式配置和历史回执兼容、完整文件与分块哈希不符、暂存篡改、限额收紧、容量预留、提交未知状态及末块恢复。JSON/TOML 面板测试覆盖新默认值、最大值保存、越界字段定位、密钥保留及修订冲突。工具指引禁止为适配限额缩图、降质、重编码或重新生成文件。

首次完整回归 **948 项：907 通过、37 失败、4 跳过**。两项失败来自工具指引超出 5,500 字符预算，其余失败出现在受限 Windows 测试进程的终止、私有配置 ACL 和相关 CLI 流程。将指引压缩至 5,444 字符后重新构建，并以正常本机权限补跑全部出现失败的 14 个测试文件及 7 个本次相关测试文件，最终 **187 项：186 通过、0 失败、1 跳过**，退出码 0。原有失败项均已覆盖复测；这不是第二次全量零失败结果。符号链接权限跳过不计为通过，本轮没有请求 UAC。

本机配置通过面板使用的配置服务完成修订校验和原子保存，只更新分块大小与缓存预算；先建立私有备份，保存后核对其他配置完全相同。没有重启真实服务。提高本机限额不代表 ChatGPT 能可靠生成或转发相同大小的 Base64 参数；真实宿主回传原图仍需用户验收，不能用这些合成协议测试代替。

## 0.16.0-preview.4 跨工作区直接复制

2026-09-11，Windows / Node.js 22.16.0。真实 preview.3 诊断确认该次本机复制请求走到了 `fs_import_file`，返回 `FILE_IMPORT_SOURCE_DENIED`，没有调用 CMD。旧批量文件工具只支持同工作区复制，跨工作区依赖模型正确选择命令，属于功能和路由缺口。

新增公开 `fs_copy`：源/目标分别按工作区及相对路径指定，执行关闭、源只读时仍能复制。源/目标身份和源 SHA-256 在提交前重新核对；目标执行现有路径、权限、预期哈希、备份、完整字节验证及持久操作状态规则。修复多路径锁先规范化再去重排序，避免路径别名重复加锁。导入接口继续限制远端来源，本机路径拒绝时明确返回 `local_file_reference` 和 `fs_copy` 恢复指引，不放宽下载策略。

验证结果（各组存在重叠，不直接相加为全量测试总数）：

| 验证 | 结果 |
| --- | --- |
| TypeScript 与组件构建 | 通过，组件继续内置 182 项资源 |
| 复制、文件写入、持久回执、批处理 | 首次七文件 87 项：86 通过、0 失败、1 权限跳过；后续新增两项持久化测试通过。复制专项最终为 18 项：17 通过、0 失败、1 权限跳过 |
| 真实 stdio 复制 | 2/2 通过，跨中文及空格目录复制 1,610,783 字节合成 PDF；源只读、执行关闭，核对两端哈希、操作状态及幂等 |
| 导入拒绝、路由、协议诊断、组件兼容 | 67/67 通过，无跳过 |
| 受工具数量影响的集成及分块回存 | 67/67 通过，无跳过；正常本机权限运行，覆盖先前受限进程中配置 ACL 报 EPERM 的两项 |
| 公开工具 schema | 61 项：53 普通、8 组件私有；导出并校验 |

一项跳过因当前 Windows 进程不能创建符号链接，不计为通过；本轮未请求管理员 UAC，未重跑全仓库回归。历史回执测试覆盖状态存储重开、源工作区重绑、完成回执落盘失败以及重启后 `committing` 转 `unknown`，不盲目重新复制。

另通过新版真实 stdio MCP 完成用户明确指定的本机 PDF 复制：先核对用户提供的源文件大小和 SHA-256，再以只创建模式调用 `fs_copy`，`operation_status` 为 done；目标 1,610,783 字节且完整摘要与源一致，源未改变。该验收使用独立状态目录，源只读且命令执行关闭，没有修改真实配置或重启正在运行的隧道。真实文件仅留在用户指定本机目录，未加入提交；公开记录不包含正文、文件名或私人路径。此结果证明 MCP 本机复制链路，不代表当前 ChatGPT 已刷新并选择新工具。

## 0.16.0-preview.3 本机 CMD 复制

2026-09-11，Windows / Node.js 22.16.0。通过真实 MCP Client 和隔离合成文件复现：显式 `cmd.exe /d /c` 接收包含路径引号的单条命令时，Node 默认 CRT 参数转义会破坏 CMD 命令文本；分参数形式原本可以复制。新版仅在显式 Windows `cmd.exe` 的 `/c` 或 `/k` 后恰有一条命令参数时采用 CMD 的 `/s` 引号规则，其他原生程序继续使用原有参数传递，不隐式开启 shell。

实际 MCP 验证四种调用形式：分参数、单命令字符串、显式 `/s`、预设固定参数。源文件和目标路径包含中文、空格及 `&`，合成原字节包含 NUL、0xFF 和 0x1A；每种复制均退出成功且逐字节一致，同键返回原 job ID。普通 Node 参数中的 shell 特殊字符和引号保持字面值。命令工具及首段指引明确区分本机复制与远端附件回存，诊断保留程序不存在、无效入口等固定错误码，不记录参数或任意异常文字。

构建通过。相关专项 **49 项：47 通过、0 失败、2 项 Windows 符号链接权限跳过**；作业取消、超时、观察、stdin 和协议补测 **33/33 通过**，合计 **82 项：80 通过、0 失败、2 跳过**。首次受限环境中的超时用例返回 `taskkill` 退出 1，使用普通本机权限重跑后该项通过，未放宽超时断言或改动终止逻辑。未请求管理员 UAC，未重跑全仓库回归；60 项工具 schema 导出和一致性检查通过。

真实配置只读检查显示执行已开启且策略为 all；近期 preview.2 记录只有文件查询，未见用户所述的本次执行或导入请求，不能据此确定宿主拦截原因。上述复制验证全部使用临时合成文件，没有复制用户 PDF、修改真实配置或重启现有服务；本地通过不代表真实 ChatGPT 已提交并完成用户的复制任务。

## 0.16.0-preview.2 执行总开关与字段校验

2026-09-11，Windows / Node.js 22.16.0。使用独立合成配置和真实 Edge 浏览器复现旧页面的问题：填写非法 SHA-256 后切回概览保存，页面仅显示 `PANEL_CONFIG_INVALID`，没有字段标记，也不切换到出错页面。此样本用于复现相同显示缺陷，不代表用户当时填错的就是这个字段。

新版实际浏览器验证通过：顶部可点击错误列表和字段旁红字明确要求 64 位十六进制字符，自动切换到连接设置并聚焦字段；修正后可以校验。通过页面开启命令执行并保存，磁盘配置实际写入 `mode=trusted-host` 和 `commandPolicy=all`，原有密钥保留；再填写大于最大值的默认超时，会明确标出两个关联字段并阻止保存、重启。错误消息只使用固定字段和文案，不返回密钥、提交值、未知属性名或原始异常。

执行实现接受本机原生程序名及绝对路径，保留关闭模式、只读工作区、超时、并发、幂等和输入上下文检查。旧配置未声明策略时保持 allowlist；配置预设和固定参数继续兼容。原生解释器符号链接验证目标后保留所选入口，避免破坏虚拟环境。新增通过真实 MCP Client 调用 `exec_start` 的绝对路径测试。

五个页面在 1440×1000 桌面和 390×844 窄屏检查通过，无横向溢出、JavaScript 或 CSP 错误；校验不写配置，实际保存后配置与页面一致。所有操作使用临时合成目录，未修改真实用户配置、开启真实命令权限或重启现有服务。公开候选检查仍仅报告原有 本机验收图片 二进制复核项，仓库检查专项 4/4 通过。

完整回归共 **909 项：906 通过、0 失败、3 跳过**，退出码 0。三项跳过均因 Windows 当前进程没有创建符号链接的权限，不计为通过；本轮未请求管理员 UAC。完整回归运行后，针对最后一处 Codex 数据目录帮助文案调整重新构建，并补跑管理页面 **20/20 通过**及真实 Edge 表单流程：错误标记、跨页面聚焦、修正后校验、执行总开关保存和跨字段超时校验均通过，页面无 JavaScript 错误。该补测不描述为第二次完整回归。60 项工具 schema 导出与一致性检查通过；GitHub 三平台 CI 尚未针对本次未提交修改运行。

## 0.16.0-preview.1 本地控制中心

2026-09-11，Windows / Node.js 22.16.0。构建通过，完整回归共 **882 项：878 通过、2 失败、2 跳过**。两项失败均为既有文件组件集成测试要求的固定说明句缺失；恢复 `content_processing:none describes server-side processing only.` 后重新构建，相关 **44 项全部通过**，覆盖组件 stdio/HTTP、MCP 诊断、稳定性、管理页面/API 和真实 CLI 生命周期。未再次运行全量，因此不将补测描述为第二次全量零失败。两项 Windows 文件符号链接测试因创建权限不足跳过，不计为通过；未请求管理员 UAC。

控制中心专项覆盖 JSON/TOML 保留、密钥只写更新、配置修订冲突、目录失效后的修复入口、权限扩展确认、管理接口鉴权与同源限制，以及配置保存与重启的版本一致性。真实 CLI HTTP 测试完成启动、就绪、执行作业、作业活动时拒绝重启、作业结束、保存新配置、重启载入新版本、停止释放租约；同时验证占用端口及配置版本不符时正常退出。隧道生命周期使用隔离的合成客户端检查取消与启动锁清理，未替代真实 ChatGPT 接入验收。

最终复核修正 HTTP 就绪的页面提示，避免把本机接口成功启动说成已连接 ChatGPT；新增实际运行传输方式与未保存草稿的交互回归。重新构建后，管理页面与 API **20 项补测全部通过**，未改动服务生命周期逻辑。

随后将 v2 `connect` 接入同一管理启动器，启动时在终端独立输出一次带临时凭据的完整链接。最终构建通过，追加相关 **46 项测试全部通过，无失败或跳过**：真实 CLI 的 HTTP 自动启动、鉴权、配置保存/重启、关闭释放租约、页面端口占用避让、与 HTTP 端口相同的避让、失败后保留页面重试、外部实例保护；合成原生隧道的自动连接及退出后保留页面；`--no-panel`、`--doctor-only` 兼容及 stdio MCP 集成。未把测试进程输出中的临时链接或密钥写入公开记录，未启动或重启真实用户连接。该追加测试未重跑完整仓库回归；公开候选检查仍只有上述既有 PNG 二进制复核项。

使用已安装 Edge 的隔离无头浏览器，对 5 个页面分别检查 1440×1000 桌面与 390×844 窄屏布局，无横向溢出、JavaScript 或 CSP 错误。通过实际表单校验与保存合成配置，验证校验不写入、保存落盘且保留密钥、页面不显示密钥正文；截图和日志保留在被忽略的本机运行目录。

另在 8777 端口启动管理页面，只读核对当前配置：版本为 0.16.0-preview.1、识别 2 个工作区、已存密钥不返回、当前服务标记为外部启动器且不受本面板管理。没有保存或修改真实配置，没有启动第二个 MCP App/StateStore，也没有停止或重启现有隧道。已请求在内置浏览器打开页面，但 UI 工具返回排队状态，不能据此声称页面已在用户窗口显示。

60 个工具（52 普通、8 组件私有）的 schema 导出与一致性检查通过，`git diff --check` 通过。公开候选检查仍仅报告既有 本机验收图片 的二进制复核项，未删除或豁免该文件。GitHub 三平台 CI 尚未针对本次未提交修改运行。服务重启前的作业检查仅反映检查时刻，尚不提供新请求的原子排空保证；使用边界见[本地控制中心](local-panel.md)。

## preview.7 无 URL 原字节回存

2026-09-11，Windows / Node.js 22.16.0。全量运行 **837 项：833 通过、2 失败、2 跳过**。两项失败指向同一问题：新增续传指导使 `SERVER_INSTRUCTIONS` 超过既有 5,500 字符预算。压缩重复说明至 5,399 字符，保持该预算和必要恢复规则；重新构建后，相关 **42 项补测全部通过**，包括 MCP 指引/诊断、HTTP、统一集成、单次二进制写入、分块恢复和真实协议 PPTX 测试。未把这次补测表述为第二次全量零失败；2 项 Windows 文件符号链接权限测试仍跳过，不计为通过。没有弹出管理员 UAC，也未改动真实隧道。

新增默认路线为 `fs_write_binary_chunk/status`，小载荷可用 `fs_write_binary`，不需要 URL。测试覆盖严格 Base64、块/整文件摘要、字节大小、持久暂存与重启、重复块、末块落库后提交前中断、Windows 子目录恢复、当前配额收紧、只读/设备校验、版本冲突和不确定结果冻结。来源拒绝诊断提供固定阶段/原因；本次真实旧导入收到 `sandbox:/...`，返回 `FILE_IMPORT_SOURCE_DENIED`，不是实际文件下载成功。

合成 PPTX 为 **170,732 字节、14 块**，通过真实 MCP 内存传输调用写入器、状态和 `fs_stat`，核对原字节、完整 SHA-256、ZIP 中央目录/每项 CRC 和正文。另用独立 `python-pptx` 与 `zipfile` 打开写回产物，确认 1 页和 ZIP 校验有效；原件与回存件哈希一致。它仅含合成内容，不是用户的论文 PPT，不证明真实 ChatGPT 已成功提交 227,644 个 Base64 字符。

60 个工具（52 普通、8 组件私有）的公开 schema 与当前验收指引已生成/校验。仓库检查专项 4/4 通过；公开候选检查仍只报告原有 本机验收图片 的二进制复核项，未删除或豁免该文件。真实配置只读 doctor 在 preview.7 通过，组件清单和 SHA-256 有效；没有初始化第二个真实 App/StateStore 或输出凭据。

内置浏览器能够枚举验收标签，但读取页面再次超时。真实 PPT 仍在 ChatGPT 的生成环境，**尚未通过新版 MCP 写回本机**。下一步需正常重启服务、更新工具，并使用同一原文件完成最终 saved/verified 与磁盘哈希核对。无需 URL、扩展或新增公网服务；服务端限额不是宿主上下文或调用预算保证。

## preview.6 稳定性修复

2026-09-11，Windows / Node.js 22.16.0。全量运行 803 项，初次结果 798 通过、3 失败、2 跳过；3 项失败为旧版手工上传指引及迁移默认值的测试预期。同步预期并修正原文件结果中残留的 PDF `next_step` 后，相关 60 项补测全部通过（包含 stdio/HTTP、完整原字节传输、组件旧资源兼容、PDF 工具、操作状态、JSON/TOML 迁移及组件并发构建）。其余已通过项未无变化重跑全套，因此这里不将补测说成第二次全量零失败。

新增重点覆盖：32 MiB/10 MiB 导入→复制→移动→删除→恢复逐字节核验；临时 URL 刷新、同键并发、下载前失败重试、重试额度收紧；写入后进程中断/回执落库失败的 unknown 与变更关联；目录状态观察；旧回执不重放；Windows 同版本组件发布竞争；安装脚本外置配置、中文路径及 ResolveOnly 无副作用。二进制批处理专项 29/29、文件操作专项 58/58、安装/组件专项 11/11，均在全量前通过。

本机实际统一配置的只读 doctor 通过，版本 0.15.0-preview.6，Git/rg 与 4,379,145 字节组件的清单和 SHA-256 均有效。检查未初始化真实 App/StateStore、修改配置或重启隧道。普通沙箱不能读私有配置 ACL 时，使用普通本机权限检查；没有弹出管理员 UAC。2 项 Windows 文件符号链接仍因缺少创建权限跳过，不算通过。

57 项工具（49 普通、8 组件私有）及 [当前验收入口](current-acceptance.md)由同一导出生成并校验；仓库检查专项 4/4 通过。公开候选检查仍只报告既有 本机验收图片 的二进制人工复核项，未删除或豁免该文件。实际 GitHub 三平台 CI 尚未针对这些未提交修改运行。

本轮内置浏览器可枚举现有 ChatGPT 标签，但读取验收会话超时。真实 PDF 正文和 ChatGPT 生成附件回存尚未完成验收，等待正常重启、工具更新及实际调用；没有据此声称任何用户 PPT 已保存。公开测试说明不包含真实配置、签名文件 URL 或文件标识。

## preview.5 生成文件回存

`0.15.0-preview.5` 增加官方 `openai/fileParams` 文件输入的 `fs_import_file` 和只返回大小/哈希的 `fs_stat`。2026-09-11，Windows / Node.js 22.16.0，77 项回存专项全部通过：原字节写入 12、下载与代理 57、导入服务和 MCP schema 7、合法 PPTX 完整本地链路 1。56 个工具 schema 导出及校验通过。

PPTX 用源码构造合法 OPC ZIP，测试仅模拟 DNS/HTTPS 的远端响应，实际调用 MCP、下载器、写入、查询、覆盖及恢复；核对原字节、ZIP CRC、SHA-256 和幻灯片正文，且持久化记录不含签名 URL/文件标识。另用独立 `python-pptx` 和 `zipfile` 打开保留的测试产物，确认一页、一个母版、Blank 布局及中文测试短语。临时产物在被忽略的本机运行目录，未作为公开二进制 fixture 加入仓库。

完整回归在普通本机权限下共 768 项：766 通过、0 失败、2 项 Windows 文件符号链接权限跳过，退出码 0；没有请求管理员 UAC 或改动真实隧道。首次受限沙箱运行的失败包括进程清理/配置 ACL 限制和漏列 preview.4 的旧资源兼容测试；补齐兼容预期后，使用正常本机权限重跑全套获得上述结果。仓库公开候选检查仍只有既有 本机验收图片 的二进制复核项，未改动该文件。

网络只读检查中，直连官方下载主机在 TLS 前被重置；复用本机已有代理后，官方主机根路径返回 HTTP 404，证明已通过 TLS 到达 HTTP 服务，不代表任何具体文件下载成功。检查未发送真实文件 URL 或 API key，也未改动本机配置。

真实 ChatGPT 生成附件是否进入 `file` 参数仍待重启后的宿主验收。内置浏览器可列出标签，但读取验收会话超时；不能将上述本地证据描述为用户的 PPT 已保存。验收流程见[生成文件回存](file-writeback.md)。

## preview.4 内置解析资源

`0.15.0-preview.4` 将固定 PDF.js 的 14 项标准字体和 168 项 CMap 内置到组件，构建时逐一校验依赖大小和 SHA-256；浏览器按需解码并验证，再向 PDF.js 提供副本。当前组件不再调用 `document_widget_asset`，旧工具仅供兼容；不引入 CDN、扩展、WASM 或浏览器解压 API。资源错误在 PDF.js 包装后仍保留有限白名单中的原始错误码及详情，缺失资源不能静默返回成功页。

实际 SDK、严格串行宿主模拟和捆绑解析器联调增加英文及中文四页 PDF，直接禁止资源 RPC，验证第 1 页和后续第 4 页正文。组件按版本生成，额外隔离测试检查运行中的模块不会改用另一个版本文件，并验证最终 HTML 大小预算。

2026-09-11，Windows / Node.js 22.16.0，构建通过：JS 4,379,145 字节，最终 HTML 4,380,308 字节，均低于 5 MiB 本地上限；182 项内置资源原字节共 1,940,006 字节。92 项专项全部通过、无跳过：服务 26、SDK 集成 11、响应归一化 10、无资源 RPC 的完整链路 6、组件 24、旧资源读取 6、内置资源 6、版本文件隔离 1、资源地址兼容 2。内存篡改真实字体而保留预期 SHA 时明确拒绝且不缓存。54 项工具 schema 导出与校验通过。

完整回归 `npm test` 共 691 项：689 通过、0 失败、2 项 Windows 文件符号链接权限跳过，进程退出码 0；未请求管理员/UAC。公开候选文件检查仍仅有既有 本机验收图片 的二进制复核项，未改动该图片。扩大后的真实 ChatGPT 组件加载和 PDF 正文仍待验收。

## preview.3 校验诊断与兼容验证

`0.15.0-preview.3` 在文件和资源的末块允许空 `next_offset` 被省略，非末块或显式错误结束位置仍拒绝，所有字节和 SHA-256 校验保留。失败结果增加严格白名单组件版本、阶段、错误码和具体校验详情，避免只返回笼统的 `FILE_INTEGRITY_ERROR`。

本地模拟已复现：只从有效末块删掉 `next_offset: null`，旧版即返回 `file / FILE_INTEGRITY_ERROR`。这不是已取得的真实宿主响应。实际 SDK + 浏览器捆绑程序 + PDF.js + 原文件及字体分块的完整联调增加真实 JSON 序列化，覆盖平铺、嵌套、二者省略空值以及仅嵌套外层省略空值，共五种边界。

2026-09-11，Windows / Node.js 22.16.0，构建通过，组件 1,766,241 字节；54 项工具 schema 已导出并校验。专项共 137 项全部通过、无跳过：PDF 服务 26、SDK 集成 11、响应归一化 10、完整链路 5、PDF 组件 21、旧原文件组件 62、旧资源兼容 2。旧原文件组件也同步 EOF 兼容，覆盖两条工具通道、空文件与多块文件，并拒绝错误范围、身份、字节和摘要。

完整回归 `npm test` 共 680 项：678 通过、0 失败、2 项 Windows 文件符号链接权限跳过，进程退出码 0；未请求管理员/UAC。

真实 ChatGPT 复验失败：服务和组件版本均为 preview.3，原文件校验及 PDF 加载已通过，`extract` 阶段返回 `ASSET_UNAVAILABLE`，0 页正文可用。只读服务日志（UTC）记录原件 chunk 在 06:28:56.176 到达，34 毫秒完成；失败 submit 在 06:29:07.370 到达；唯一资源请求直到 06:30:29.892 才到达，72 毫秒成功完成。失败前后共 9 次普通读取，最后一次正确返回文档级错误。依赖中 186 项固定资源本地读取和哈希均通过。

将相同合成 PDF 的有效字体回复延迟超过 10 秒，可用实际 preview.3 捆绑组件复现相同 `extract / ASSET_UNAVAILABLE`。旧组件两层异常处理会吞掉 `HOST_TIMEOUT`，晚到成功回复不会恢复失败文档。真实日志支持组件超时后资源请求才抵达的解释，但没有记录浏览器发出时间，尚不能确定延迟发生在 ChatGPT、审批或隧道的哪一层。

## preview.2 修复验证

`0.15.0-preview.2` 修复响应包装中的私有字节丢失、首个页请求失败未传播到其他范围、已完成页面子集仍重复排队的问题，并补充公开的安全进度观察。60 项专项全部通过：服务 22、SDK 集成 9、响应包装 8、实际完整链路 2、浏览器组件 17、旧资源兼容 2。

2026-09-11，Windows / Node.js 22.16.0，完整回归 `npm test` 共 662 项：660 通过、0 失败、2 项 Windows 文件符号链接权限跳过，进程退出码为 0。类型检查与构建通过，组件为 1,764,936 字节，54 项 MCP 工具 schema 导出与一致性检查通过。未请求管理员/UAC，未修改运行中的真实服务。

真实 ChatGPT 复验失败：用户核对 `system_status` 与组件版本均为 `0.15.0-preview.2`，组件显示 MCP Apps 工具通道已声明，阶段 `file`，错误 `FILE_INTEGRITY_ERROR`。普通读取正确返回文档级错误：1 次组件 poll、1 次 chunk、服务端已提供 2489/2489 字节、0 个成功页请求；用户立即停止，没有继续请求第 4 页。服务端提供全部字节不代表浏览器收到或校验通过。未取得该次具体响应字段，无法从此错误区分编码、摘要或结束位置校验失败；本地通过不代表真实 ChatGPT 已取得 PDF 正文。

## preview.1 本地验证与网页失败

2026-09-11，`0.15.0-preview.1`，Windows / Node.js 22.16.0：

| 检查 | 结果 |
| --- | --- |
| TypeScript、组件构建 | 通过；PDF.js 6.3.289，生成组件 1,762,252 字节，保留依赖许可 |
| PDF 服务 | 17 通过：完整原件、设备/工作区授权、期限、缓存、稳定分页任务、截断与失败状态 |
| PDF 资源 | 6 通过：精确白名单、真实字体/CMap/WASM 哈希、分块与缓存预算、路径拒绝 |
| SDK 集成 | 7 通过：公开/私有工具、元数据隔离、生命周期与真实回环 HTTP 严格串行调度 |
| 浏览器组件 | 13 通过：实际捆绑 PDF.js 读取英文多页和中文 PDF、真实字体/CMap、整文件校验、截断、兼容桥接、超时不重放及空闲退避 |
| 完整回归 `npm test` | 641 项：639 通过、0 失败、2 项 Windows 文件符号链接权限跳过；未请求管理员/UAC 或修改真实服务 |
| 追加完整链路 | 1 通过：该测试在上述回归启动后新增并单独执行。实际 SDK + 612 KiB 四页原 PDF + 三次分块 + 私有字体 + 捆绑浏览器解析 + 提交 + 普通工具正文，严格串行并验证后续第 4 页 |
| MCP schema | 隔离 stdio 导出并校验 54 项 |
| 新网页验收样本 | 独立四页合成 PDF 已由同一捆绑解析器读回，整件哈希匹配；未使用真实论文 |
| 仓库候选检查 | 仍有 1 项既有未审查图片 本机验收图片；未修改、忽略或发布该图片 |
| 真实 ChatGPT PDF | 失败：第 1 页和第 4 页共 60 次 document_read 均未取得正文；读取限额用尽后停止 |

该版源码共验证 642 项用例：640 通过、0 失败、2 项权限跳过；上述总数包括单独执行的 1 项追加链路测试。真实 ChatGPT PDF 验收不能由本地链路或下面的合成文本成功推断。

该次真实日志（UTC）：05:21:13.597 open、14.460 资源读取、17.367 组件 poll、18.395 chunk、19.491 submit；后续没有 asset 或 poll。60 次公开读取从 05:21:16.702 持续到 05:24:16.562。所有 RPC 均成功回复；但 submit 也可接受失败码，RPC 成功不能说明提交的是正文。诊断没有保存参数、正文或该次组件错误码。

复查发现：此前完整联调只模拟平铺工具响应。把相同有效结果放入「外层公开 structuredContent + 内层带私有字节的 mcp_tool_result」时，旧组件实际复现 poll → chunk → FILE_INTEGRITY_ERROR 提交，且不调用字体资源。这是可重现代码缺陷，与真实时序吻合；在取得组件诊断前，仍不能断言实际宿主一定使用了此包装。

## preview.4 本地结果与真实成功

2026-09-11，`0.14.0-preview.4`，Windows / Node.js 22.16.0：

| 检查 | 结果 |
| --- | --- |
| TypeScript 构建 | 通过 |
| 非阻塞回传专项 | 40 通过：状态存储 14、组件脚本 18、MCP 集成 8 |
| 严格串行调度 | 通过：依次完成 read(pending)、poll、submit、read(ready)，不允许调用重叠；同时覆盖真实回环 HTTP |
| 提前提交与有界查询 | 通过：首次公开读取前可以提交；6 次 pending 观察后停止；限额不清除任务或遮蔽已就绪结果 |
| 旧组件资源兼容 | preview.3 URI 返回当前组件；同样只允许固定资源地址 |
| MCP 工具 schema | 隔离 stdio 已导出 48 项 |
| 完整回归 `npm test` | 598 项：596 通过、0 失败、2 项 Windows 文件符号链接权限跳过 |
| 真实 ChatGPT | 用户截图确认两次 ready，分别引用不同的实际回传文本；同一运行实例诊断记录对应 2 open、2 poll、2 submit、4 read，全部成功 |

本版没有引入 PDF.js、外部资源、浏览器扩展、自动 follow-up 消息或上传按钮。已保留上一版的真实失败证据及来源限制。

## preview.3 回归与真实失败

2026-09-11，`0.14.0-preview.3`，Windows / Node.js 22.16.0：

| 检查 | 结果 |
| --- | --- |
| TypeScript 构建 | 通过 |
| 新增合成回传专项 | 34 通过：状态存储 11、组件脚本 17、MCP 集成 6 |
| 真实回环 HTTP 并发 | 通过；模型 read 等待期间 poll/submit 可完成独立 POST |
| 浏览器脚本与状态存储联调 | 通过；使用 WebCrypto 计算合成文本，并使等待中的普通工具返回实际内容 |
| MCP 工具 schema | 隔离 stdio 导出并校验 48 项；不使用运行中服务的数据库 |
| 仓库检查脚本专项 | 4 通过 |
| 完整回归 `npm test` | 592 项：590 通过、0 失败、2 项 Windows 文件符号链接权限跳过 |
| 公开候选文件检查 | 1 项待核查：已有 本机验收图片 命中二进制文件复核规则；未改动或加入白名单 |
| 真实 ChatGPT 基础调用 | 已确认 `0.14.0-preview.3` 的两次 `system_status` 成功；北京时间 11:08:22 的 `tools/list` 正常返回 |
| 真实 ChatGPT 回传验收 | 失败：后续已实际触发 open/read；read 等待 8033ms 后返回 RELAY_TIMEOUT，第一次组件 poll 在超时后 28ms 才到达；没有 submit |

新专项覆盖凭据隔离、重复通知、提交顺序、已完成结果缓存、迟到提交、超时、取消、过期、关闭清理和安全错误码。它只证明本地合成回传行为；未安装 PDF.js、未解析 PDF、未证明原生附件或模型可用的页图像。详见[原型说明](reading-relay-prototype.md)。

preview.3 的本机并发测试未覆盖真实连接观察到的串行调度，这是验收缺口。服务端日志只能说明组件请求在公开读取结束后到达，不能单独区分延迟发生于 ChatGPT 还是隧道层。preview.4 删除了公开工具中的等待依赖，并增加严格串行回归。

## 上一版基线

2026-09-10，0.14.0-preview.2，Windows / Node.js 22.16.0：

| 检查 | 结果 |
| --- | --- |
| TypeScript 类型检查与构建 | 通过 |
| 核心完整回归 `npm test` | 558 项：556 通过、0 失败、2 跳过 |
| 仓库检查脚本专项 `npm run test:repo` | 4 通过、0 失败 |
| MCP 工具 schema | 已从隔离的本地 stdio 服务生成 44 项 |
| 全新源码安装检查 | 10 个步骤通过，包含锁定依赖安装、构建、init、validate、doctor、schema 与仓库检查 |
| 公开候选文件检查 | 未发现命中规则的凭据、私有文件或失效本地文件链接 |
| npm 生产依赖安全公告检查 | 0 项已知漏洞；仅代表本次查询结果 |

两项跳过为 Windows 文件符号链接场景，当前进程没有创建该类链接的权限。没有为本次整理提升为管理员、开启开发者模式或复用此前的一次性 UAC 授权。

全新安装检查使用临时的中文与空格路径，仅复制公开候选文件，不带旧 node_modules、dist、真实配置或状态。依赖按锁文件从本机缓存重新安装，随后按 README 初始化；测试结束清理该临时副本。它验证本机安装流程，不代替其他平台或无缓存网络下载验收。

本地测试覆盖实际临时文件、SQLite、程序/子进程、HTTP/stdio、配置及合成会话。它们**不等于当前版本已完成真实 ChatGPT 网页验收**，也不证明原文件正文附件能力已经实现。

## 日常运行

```text
npm ci --ignore-scripts
npm run check
npm test
npm run check:tools
npm run test:repo
npm run audit:repo
```

`npm test` 先构建，按当前 `test/*.test.ts` 源文件清单运行，避免已删除实验遗留的 dist 测试被重新执行。测试串行调度以减少本机进程/端口冲突。

测试使用临时目录、合成内容和测试自己的进程，不加载真实 `.webcodex/config.toml`，不需要真实 API key 或隧道。某些受限运行环境会阻止 Windows ACL 检查、回环端口或子进程清理；应在具备这些本机能力的开发环境运行，不能把权限失败直接视为断言通过。

旧 PowerShell 兼容入口另有 `scripts/test-start-tunnel.ps1`、`test-check-tunnel.ps1` 和 `test-tunnel-auth.ps1`。修改这些旧脚本时单独运行相应测试；不能宣称核心 Node 回归已运行所有额外脚本。

## 覆盖范围

- 文本/Unicode、分页/长行、大文件边界、补丁、哈希冲突、幂等、备份与条件恢复。
- 工作区身份、设备匹配、同权限重叠、只读/离线、链接路径与显式 Git worktree。
- JSON/TOML、路径展开、私有配置权限、迁移、执行别名/profile 和环境限制。
- 程序输出与 stdin、超时/取消、作业观察、任务修订、交接和重启后的不确定状态。
- Codex 可见会话分页、搜索、脱敏、项目匹配及支持的历史格式。
- MCP stdio/HTTP 调用、空参数、输出 schema、有限历史组件 URI 及诊断。
- 官方隧道启动器的验证/锁/健康判断，使用合成客户端；可选本地面板及旧实验配置不激活。

自动化测试通过不排除未覆盖缺陷。真实文件格式、Codex 内部日志结构和 ChatGPT 宿主接口变化仍可能需要兼容修复。

## Windows 符号链接专项

在已经拥有文件符号链接权限的终端运行：

```text
npm run test:symlinks
```

它严格要求两项均通过，不能用跳过满足要求。脚本不会修改系统设置，结果留在被忽略的本机运行目录。通常需要已启用开发者模式的环境或用户自行授权的管理员终端；普通 directory junction 测试不能替代文件符号链接场景。

## 跨平台 CI

GitHub workflow 配置 Windows、Ubuntu、macOS，固定 Node.js 22.16.0，并设置 `WEBCODEX_REQUIRE_FILE_SYMLINK_TESTS=1`。还检查工具文档是否过期、仓库检查专项和公开候选文件。

远端结果见 [GitHub Actions](https://github.com/xq3427/WebCodex/actions/workflows/ci.yml)。上方统计为本地 Windows 结果，不能代替某次提交的三平台运行结果；应核对对应提交的完整矩阵。失败必须修复或明确收窄支持范围，不能为绿灯取消必要检查。

## 发布前人工验收

创建 GitHub 仓库后核对 CI，并在具备权限的真实 ChatGPT 连接中验证设备/工作区、文本创建及读回、修改冲突和恢复、Codex 接续，以及用户已启用时的程序执行。

原文件自动上传暂停，不纳入已完成能力。不要用本地组件、传输字节、上传回执、文件卡片或旧版本网页截图代替新版本正文读取证据。
