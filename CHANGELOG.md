# 更新记录

## 0.16.0-preview.17 — 2026-09-21

- 修复官方 tunnel-client 安装清单把 `executable` / `archive` 写成安装机器绝对路径的问题；新安装改为保存相对 `toolsDir/tunnel-client` 的可移植路径，换目录或另一台电脑重新安装后不会因旧机器盘符/路径被误判为 `TUNNEL_CLIENT_INVALID`。
- 保留官方 release 来源、平台/架构、目录边界、SHA-256 和执行权限校验；旧版绝对路径清单仅在仍指向当前受控安装目录内部时继续兼容。
- setup 与 Tunnel 合成测试改用相对路径清单，并核对记录路径可解析回实际安装文件。
- npm 发布验收现在实际执行 `webcodex --help` 与 `webcodex-mcp --help` 两个安装后入口；快速开始区分全局 `npm install -g` 和项目内 `npm install`，避免把未进入 PATH 的本地安装误报为命令缺失。

## 0.16.0-preview.16 — 2026-09-16

- npm 包新增 `webcodex` 命令入口，与现有 `webcodex-mcp` 指向同一 CLI；简短命令可直接执行 `webcodex init/connect/access`，旧命令保持兼容。
- 发布包安装测试同时核对两个命令入口，避免文档中的启动命令与 npm 实际注册名称再次不一致。
- 修复异常退出遗留的 Tunnel/daemon 诊断锁被控制面板永久误判为“外部服务仍在运行”：只有 SQLite 所有权锁或可验证的活动隧道仍存在时才保留外部状态；已确认失效的记录交给原有安全恢复流程归档并重新连接。

## 0.16.0-preview.15 — 2026-09-16

- 将 Windows/Linux 只读文件系统错误 `EROFS` 与 `EACCES`、`EPERM` 一致归类为操作系统访问拒绝，避免把磁盘、挂载或账户权限问题误报为未知内部错误。
- 写入权限错误现在明确说明：`trusted-host + all` 只开放 WebCodex 本机策略，不会提升系统账户、绕过 NTFS/共享/挂载权限或自动触发管理员授权。
- 快速开始和控制中心排错文档补充 ChatGPT 工具调度拦截与本机文件系统拒绝的区分方法，并建议代码修改优先使用专用文件工具、同机复制优先使用 `fs_copy`。

## 0.16.0-preview.14 — 2026-09-15

- 修复新 Windows/npm 安装虽可读取但无法写入或运行命令的默认权限落差：`init/setup` 创建的初始工作区直接可读写，命令策略使用 `trusted-host + all`，无需逐项放行本机程序。
- 控制中心新增“一键完全开放”和真实权限检测；检测会在每个可写工作区创建、读回、删除随机临时文件，并启动一个无 shell 的本机 Node 子进程，分别报告 WebCodex 策略和 Windows/NTFS 账户权限问题。
- 新增 `webcodex-mcp access full` 与 `webcodex-mcp access check`，方便无界面设备修复旧配置。升级不会静默扩大已有配置；全部命令仍以启动 WebCodex 的系统账户运行，不触发 UAC 或绕过操作系统权限。
- 强化 MCP 说明：用户要求修改时应实际调用写入工具，不能用模糊的“安全检查拦截”代替真实工具名和错误码。

## 0.16.0-preview.13 — 2026-09-15

- 修复 Linux 无桌面服务器通过 SSH 本地端口转发打开控制中心时的 `REQUEST_ORIGIN_DENIED`：安全接受 `127.0.0.1` 与 `localhost` 回环别名，并允许本地转发端口与服务器面板端口不同。
- 继续要求 Host 与 Origin 使用同一回环端口、面板令牌和本机回环监听；局域网、公网及其他主机名仍被拒绝。
- 增加 Linux/SSH 转发场景的面板和管理接口回归测试，并补充端口转发排错文档。

## 0.16.0-preview.12 — 2026-09-15

- `init` 的 Tunnel/API key 向导改为只显示官方链接，由用户自行打开并将 ID/key 粘贴回终端；不读取网页、不启动浏览器自动化，适用于本机、SSH 和无桌面环境。

## 0.16.0-preview.11 — 2026-09-15

- `init` 增加首次交互式 Tunnel 向导：打印并尝试打开官方 Tunnel 页面，接收 Tunnel ID 后再打开 API keys 页面接收 API key；密钥输入不回显且不会写入日志。
- `init` 支持省略 `--workspace`，默认创建当前目录下的 `workspace`；无桌面环境可用 `--no-tunnel` 跳过向导。
- 管理页面提交 Tunnel 凭据时自动去除首尾粘贴空白，服务拒绝请求时显示安全的错误码和原因。


## 0.16.0-preview.8 — 2026-09-12

- 修复 Tunnel 启动器异常退出后遗留 `launcher.lock` 导致后续连接无法启动：仅当锁记录的进程已确认退出时自动将锁归档为 `launcher.abandoned-*.json` 并继续启动；活动进程、格式异常或权限错误仍会保留并拒绝接管。
- 新增跨平台 `setup`：首次安装自动准备 Git/ripgrep/官方隧道客户端、私有配置与工作区，打开本机管理页面；重复运行原样保留已有配置。Windows 缺少 Git 时安装官方 MinGit。
- 安装中断后可恢复已确认退出进程的安装锁；GitHub 元数据下载失败时使用随版本保存的官方资产清单，避免匿名 API 限流阻断安装，继续执行大小和 SHA-256 校验。
- 新增 GitHub 安装 ZIP：Windows 双击运行，Linux/macOS 使用 shell；缺少 Node 时安装经过官方校验的便携 Node 22.23.2。应用按版本安装，不改系统服务和全局 PATH。
- 新增干净 npm/tgz 发布流水线，显式清单打包当前源码对应的编译文件与当前组件，排除私有状态、测试、旧 dist 和源映射；锁定生产依赖。
- 补全快速开始、升级、代理、卸载和发布教程，并加入安装与包内容回归。

- 修复 Windows OpenSSH 因 `ProgramData` 在精简环境中丢失而静默退出 255：隧道、面板托管 HTTP 服务及命令子进程均保留宿主系统变量，无需新增配置或权限。
- 非零退出且完全没有捕获输出时，作业返回 `PROCESS_EXIT_WITHOUT_OUTPUT` 诊断，避免仅凭退出码推断网络或认证失败。
- 新增真实 OpenSSH 启动/stderr 回归、两种启动器的环境继承检查与 SSH 故障定位说明。实际只读 MCP 连接验证及测试范围见[测试说明](docs/testing.md)。

## 0.16.0-preview.7 跨平台修正 — 2026-09-12

- 修复 Windows 上 npm 10 执行项目脚本时缺少 `ComSpec` 而静默失败：从系统目录派生命令解释器路径，不继承父进程中的任意覆盖值。
- Windows 私有配置权限改用纯 .NET API，移除 PowerShell 模块自动加载依赖；保留私有 DACL、身份检查和超时。
- 修复 Linux/macOS 并发发布文档组件时，把完整快照误判为损坏：允许已打开的旧快照，并消除路径检查与打开之间的换代竞态；继续验证文件状态、大小上限和组件 SHA-256。
- 修复 POSIX 目录因正常的多链接计数被错误归类为链接；目录返回 `NOT_A_FILE`，普通硬链接和符号链接仍受原策略限制。
- 配置迁移的并发变更测试改为确定的提交时序，覆盖四类来源的内容变化和同字节路径替换。

首次三平台 CI 失败记录及修正验证见[测试说明](docs/testing.md)。

## 0.16.0-preview.7 — 2026-09-11

本次源码更新汇总首次发布后的文件工作流、PDF 原型及本地管理功能；仍为预览版。

- 新增本地控制中心：`connect` 输出带临时凭据的本机链接，可编辑 JSON/TOML 配置、设备和工作区、密钥、权限及限额，并管理由面板启动的服务。校验失败会定位到具体设置；本机命令执行提供统一开关。
- 新增 `fs_copy`，支持跨工作区复制完整原字节及只读源目录；修复 Windows CMD 单条命令中的路径引号处理。
- 新增 `fs_save_file` / `fs_save_file_status`，通过真实宿主文件 ID 或官方对象保存生成原件。组件私下解析临时地址，服务在写入前核对源文件大小与 SHA-256，保留目标版本检查、备份和持久操作状态。真实 ChatGPT 文件授权及完整写回仍待验收。
- 保留小载荷二进制写入和分块兼容入口。默认分块 64 KiB、可配置至 256 KiB；原图不得为满足传输限制缩小或重编码。宿主 Base64 截断已实际发生，新自动保存入口避免这种文本中转。
- 新增 PDF 文字层读取原型：完整文件在组件中校验，PDF.js 解析后通过普通 MCP 工具分页返回正文；字体和 CMap 随组件构建。真实 PDF 宿主验收仍未完成，不等同于 ChatGPT 原生附件上传，不支持 OCR。
- 强化下载来源检查、源字节校验、重复调用与未知写入结果处理，以及有限历史组件地址兼容。当前共 65 个工具，含 55 个普通工具和 10 个组件私有工具。
- 补全中英文使用说明、模块目录、配置管理、原文件回存和发布文档；本机配置、凭据、运行记录和私人验收文件不进入源码发布。

验证结果、权限跳过与真实宿主限制见[测试说明](docs/testing.md)。代码测试和合成文件测试通过不代表真实 ChatGPT 已完成原件读写。

## 0.14.0-preview.2 — 2026-09-10

为首次公开源码整理主线：

- 暂停自动原文件上传，将浏览器和 Actions/公网通道草稿从公开主线移除；不再安装 Playwright。
- 移除未完成的浏览器上传命令和四个工具，保留 44 个 MCP 工具。历史实验配置可继续解析，但不启动实验功能。
- 保留旧组件资源地址兼容，修正模型指导中的能力说明。
- 新增中英文 README、MIT 许可证、贡献和安全说明、架构/路线文档、仓库检查与发布清单。
- 统一公开配置示例、工具 schema 和版本，剔除本机验收记录、凭据与研究下载资料。

本地测试结果和未验证范围见[测试说明](docs/testing.md)。本记录不将此前未发布实验视为正式可用版本。
